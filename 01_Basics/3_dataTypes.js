"use strict"; // treat all JS code as newer version

//alert(Hello) // It has different syntax while using node so it will show error if we run here.

let Name = "Deep"; 
let age = 14;  
let isloggedin = false; 

console.log(typeof(Name));
console.log(typeof(age));
console.log(typeof(isloggedin));

/*
boolean true/false
number -> 2 to power 53
string
bigint
null -> standalone value, different from undefined
undefined
symbol

 */